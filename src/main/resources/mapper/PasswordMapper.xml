<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="us.pax.basil.mapper.PasswordMapper">

<select id="getTokenExpiration" resultType="java.sql.Timestamp">
	SELECT 
		PASS_TOKEN_EXP
	FROM 
		BASIL_SEC_PRD.USERS 
	WHERE 
		PASS_TOKEN=#{token};
</select>

<select id="getTokenExpirationByUser" resultType="java.sql.Timestamp">
	SELECT 
		PASS_TOKEN_EXP
	FROM 
		BASIL_SEC_PRD.USERS 
	WHERE 
		U_OID=#{userId}
		AND PASS_TOKEN=#{token};
</select>

<select id="getUserByIdAndToken" resultType="us.pax.basil.entity.User">
	SELECT
		U_OID as id,
		NAME,
		COMPANY_ID,
		EMAIL,
		PASSWORD,
		PASS_TOKEN as token,
		PASS_TOKEN_EXP as tokenExp,
		STATUS
	FROM BASIL_SEC_PRD.USERS
	WHERE U_OID=#{userId}
	  AND PASS_TOKEN=#{token};
</select>

<update id="saveTokenAndExpiration">
	UPDATE BASIL_SEC_PRD.USERS U
	   SET 
	   		U.PASS_TOKEN = #{token, jdbcType=VARCHAR},
	   		U.PASS_TOKEN_EXP = #{timeStamp, jdbcType=TIMESTAMP}
    WHERE EMAIL = #{email, jdbcType=VARCHAR}
</update>

<update id="saveTokenAndExpirationByUser">
	UPDATE BASIL_SEC_PRD.USERS U
	   SET 
	   		U.PASS_TOKEN = #{token, jdbcType=VARCHAR},
	   		U.PASS_TOKEN_EXP = #{timeStamp, jdbcType=TIMESTAMP}
    WHERE U_OID = #{userId, jdbcType=INTEGER}
</update>

<update id="savePassword">
	UPDATE BASIL_SEC_PRD.USERS U
	   SET 
	   		U.PASSWORD = #{password, jdbcType=VARCHAR}
    WHERE PASS_TOKEN = #{token, jdbcType=VARCHAR}
</update>

<update id="savePasswordByUser">
	UPDATE BASIL_SEC_PRD.USERS U
	   SET 
	   		U.PASSWORD = #{password, jdbcType=VARCHAR}
    WHERE U_OID = #{userId, jdbcType=INTEGER}
</update>
<update id="setStatus">
	UPDATE BASIL_SEC_PRD.USERS U
	   SET 
	   		U.STATUS = #{status}
    WHERE PASS_TOKEN = #{token}
</update>
<update id="resetToken">
	UPDATE BASIL_SEC_PRD.USERS U
	   SET 
	   		U.PASS_TOKEN = #{resetToken}
    WHERE PASS_TOKEN = #{token}
</update>

<update id="clearTokenByUser">
	UPDATE BASIL_SEC_PRD.USERS U
	   SET 
	   		U.PASS_TOKEN = NULL,
	   		U.PASS_TOKEN_EXP = NULL
    WHERE U_OID = #{userId, jdbcType=INTEGER}
</update>
</mapper>
