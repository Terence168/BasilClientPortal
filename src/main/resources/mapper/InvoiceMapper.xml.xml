<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="us.pax.basil.mapper.InvoiceMapper">
    <insert id="insertInvoiceList" parameterType="java.util.List" useGeneratedKeys="true">
        INSERT INTO BASIL_SEC_PRD.FACT_INVOICE(XM_OID, PXM_OID, MO_OID, DIAGNOSTIC, COSMETIC, MINOR, MAJOR, CREATED_DATE, INV1_ITEM_TOTAL)
        VALUES
        <foreach item="item" collection="list" separator=",">
            (
            <if test="item.xmOID == null">
                #{null},
            </if>
            <if test="item.xmOID != null">
                #{item.xmOID},
            </if>
            <if test="item.pxmOID == null">
                #{null},
            </if>
            <if test="item.pxmOID != null">
                #{item.pxmOID},
            </if>
            #{item.moOID},
            #{item.diagnostic},
            #{item.cosmetic},
            #{item.minor},#{item.major}, now(), #{item.inv1ItemTotal})
        </foreach>
    </insert>

    <update id="updateInvoiceList" parameterType="java.util.List">
        <foreach collection="list" item="item" separator=";">
            UPDATE BASIL_SEC_PRD.FACT_INVOICE
            <set>
                <if test = "item.diagnostic != null">
                    DIAGNOSTIC = #{item.diagnostic},
                </if>
                <if test="item.cosmetic != null">
                    COSMETIC = #{item.cosmetic}
                </if>
                <if test="item.minor!= null">
                    MINOR = #{item.minor}
                </if>
                <if test="item.inv1ItemTotal != null">
                    INV1_ITEM_TOTAL = #{item.inv1ItemTotal}
                </if>
                UPDATE_DATE = #{now()}
            </set>
            WHERE FI_OID = #{item.fiOID}
        </foreach>
    </update>

    <select id="getInvoiceByMoOID" resultType="us.pax.basil.entity.invoice.Invoice">
        select FI_OID as fiOID, XM_OID as xmOID, PXM_OID as pxmOID, MO_OID as moOID, DIAGNOSTIC as diagnostic,
        COSMETIC as cosmetic, MINOR as minor, CREATED_DATE as createDate, UPDATE_DATE as updateDate
        from BASIL_SEC_PRD.FACT_INVOICE
        where MO_OID = #{moOID}
    </select>
</mapper>