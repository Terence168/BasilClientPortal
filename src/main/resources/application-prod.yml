spring:
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB
  datasource:
    #  AWS PROD
    url: jdbc:mysql://prodbasilmysql.c09xunhasest.us-east-1.rds.amazonaws.com:3306/BASIL_ODS_PRD?useUnicode=true&characterEncoding=utf8&serverTimezone=GMT-5&allowMultiQueries=true&zeroDateTimeBehavior=convertToNull
    username: BASIL_ODS_PRD
    password: PaxUser@123
    
  mail:
    host: smtp.office365.com
    port: 587
    protocol: smtp
    username: "basil@pax.us"
    password: "Pax4Future!@"
    properties:
      mail:
        transport:
          protocol: smtp
        smtp:
          auth: true
          starttls:
            enable: true
          timeout: 8000

security:
  # urls which do not need authentication
  permit-all:
    urls:
      - /webjars/**
      - /swagger-resources
      - /swagger-ui.html
      - /swagger-resources/**
      - /v2/**
      - /internal/**
      - /error
      - /api/**
      - /user/**
      - /password/**
      - /privilege/**
      - /rma/**

  # CORS configuration
  cors:
    configs:
      - path: "/**"
        #        origins: 'http://localhost:8080'
        #       origins: 'http://216.238.144.197:8888/'
        origins: "*"
        headers: "*"
        methods: "*"
        allowCredentials: true
        maxAge: 3600

#  configuration:
#    map-underscore-to-camel-case: false

logging:
  config: conf/logback-spring.xml
  level:
    org.springframework.security: debug

server:
  servlet:
    context-path: /api/v1/
    session:
      timeout: 3600
  port: 8989
  tomcat:
    remote_ip_header: X-Forwarded-For
    protocol_header: X-Forwarded-Proto
    port-header: X-Forwarded-Port
    basedir: ./
    accesslog:
      enabled: true
      directory: service-logs
  ssl:
    enabled: false
    key-store: server.keystore
    key-store-password: pax@4901FL!2
    key-store-type: JKS
    #trust-store: src/test/resources/trust.keystore
    #trust-store-password: pax@4901
    #trust-store-type: JKS
    #trust-store-provider: SUN
    #client-auth: need
http_pool:
  max_total: 200
  default_max_per_route: 100
  connect_timeout: 5000
  connection_request_timeout: 1000
  socket_timeout: 65000
  validate_after_inactivity: 2000
  
frontend:  
  urls:
    activate: https://basilclientuat.paxcenters.com:8181/basil/activate-user/
    change: https://basilclientuat.paxcenters.com:8181/activate-user/
    forgot: https://basilclientuat.paxcenters.com:8181/activate-user/
    reset: https://basilclientuat.paxcenters.com:8181/activate-user/
